part of'WidgetsImportes.dart';
class BuildListItem extends StatelessWidget {
  const BuildListItem({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
